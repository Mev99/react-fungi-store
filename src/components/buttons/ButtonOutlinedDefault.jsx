import React from "react";
import { Button } from "@mui/material";

const ButtonOutlinedDefault = ({ text }) => {
  return <Button variant="outlined">{text}</Button>;
};

export default ButtonOutlinedDefault;
